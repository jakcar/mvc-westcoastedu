export class Course {
  constructor(courseNumber, title, description, duration, participants) {
    this.courseNumber = courseNumber
    this.title = title
    this.description = description
    this.duration = duration
    this.participants = participants
  }
}
